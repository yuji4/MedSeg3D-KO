# 실험 상세 (EXPERIMENT.md)

본 문서는 MedSeg-3D-KO의 3계층 한국어 변환 파이프라인 설계 과정에서 수행한 7종 실험의 상세 내용을 담고 있습니다.

**공통 실험 환경**
- 데이터셋: Task07 Pancreas (MSD), 5개 케이스 (`pancreas_001/004/005/006/010`)
- GPU: Tesla T4 (Google Colab)
- 평가 지표: Dice Similarity Coefficient (DSC)
- 베이스 모델: M3D-LaMed-Phi-3-4B

---

## 목차

| # | 실험 | 핵심 질문 | 노트북 |
|:--:|------|----------|--------|
| 1 | [번역 전략 비교](#실험-1-번역-전략-비교) | 한국어를 어떻게 전달해야 하는가? | Med3D_Korean_Interface_Pipeline |
| 2 | [표현 다양성 강건성](#실험-2-표현-다양성-강건성) | 다양한 표현에서도 안정적인가? | Med3D_Korean_Interface_Pipeline |
| 3 | [Threshold Sensitivity](#실험-3-threshold-sensitivity) | 임계값은 성능에 영향을 주는가? | Med3D_Korean_Interface_Pipeline |
| 4 | [프롬프트 최적화](#실험-4-프롬프트-엔지니어링-최적화) | 더 나은 영문 프롬프트가 있는가? | M3D_KO_Prompt_Optimal |
| 5 | [의도 분류 Robustness](#실험-5-layer-1-의도-분류-robustness) | 규칙 기반 분류기로 충분한가? | Korean_Medical_Query_Transformation |
| 6 | [Task Mismatch](#실험-6-layer-3-task-mismatch) | 템플릿 선택이 정말 필요한가? | Korean_Medical_Query_Transformation |
| 7 | [Ablation Study](#실험-7-ablation-study) | 각 계층의 기여도는 얼마인가? | Korean_Medical_Query_Transformation |

---

## 실험 1: 번역 전략 비교

**노트북:** `Med3D_Korean_Interface_Pipeline_Experiment.ipynb`

### 실험 목적

M3D-LaMed는 영어 전용 모델로, 한국어 입력을 처리하지 못합니다. 이를 해결하기 위해 가능한 접근 방식들을 체계적으로 비교하고, 어떤 변환 전략이 실제 세그멘테이션 성능으로 이어지는지 확인합니다.

### 알고자 했던 것

- 한국어를 그냥 넣으면 어떻게 되는가?
- 일반 번역기로 영어로 바꾸면 해결되는가?
- 의료 용어 사전만 있어도 충분한가?
- Layer 1+2+3 전체 파이프라인이 실제로 효과가 있는가?

### 방법

| ID | 방법 | 처리 방식 |
|----|------|---------|
| A | 한국어 직접 입력 | "췌장을 분할해줘" 그대로 전달 |
| B | 일반 번역 | GoogleTranslate → 영어로 자유 번역 |
| C | 의료 사전 정규화 (Layer 2만) | 장기명만 영문 canonical form으로 치환 |
| D | Hybrid Full (L1+L2+L3) | 의도 분류 + 정규화 + 템플릿 선택 전체 적용 |

### 결과

| 방법 | 평균 Dice | 표준편차 | 최대 | 최소 |
|------|:---------:|:--------:|:----:|:----:|
| A. 한국어 직접 | 0.0000 | 0.0000 | 0.0000 | 0.0000 |
| B. 일반 번역 | 0.0000 | 0.0000 | 0.0000 | 0.0000 |
| C. 의료 사전 (L2) | 0.5457 | 0.0314 | 0.5981 | 0.5109 |
| **D. Hybrid Full** | **0.5515** | **0.0352** | **0.6109** | **0.5037** |

**케이스별 상세**

| 케이스 | A | B | C | D |
|--------|:---:|:---:|:---:|:---:|
| pancreas_001 | 0.0000 | 0.0000 | 0.5109 | 0.5037 |
| pancreas_004 | 0.0000 | 0.0000 | 0.5343 | 0.5417 |
| pancreas_005 | 0.0000 | 0.0000 | 0.5981 | 0.6109 |
| pancreas_006 | 0.0000 | 0.0000 | 0.5220 | 0.5382 |
| pancreas_010 | 0.0000 | 오류 | 0.5630 | 0.5630 |

### 발견

방법 B의 번역 결과를 보면 "췌장을 분할해줘" → `"Split the pancreas"` 로 번역됩니다. 의미는 맞지만 Dice는 0.0000입니다. M3D는 단순히 장기명을 인식하는 것이 아니라, `"Can you segment..."`, `"Please output the mask"` 같은 **학습 시 본 특정 토큰 패턴**에 강하게 의존합니다. 따라서 의미가 아무리 올바르더라도 형식이 맞지 않으면 `[SEG]` 토큰 자체를 생성하지 않습니다.

방법 C는 장기명 정규화만으로도 Dice 0.5457까지 도달합니다. 이는 **엔티티 정규화(Layer 2)가 가장 핵심적인 계층**임을 시사합니다.

---

## 실험 2: 표현 다양성 강건성

**노트북:** `Med3D_Korean_Interface_Pipeline_Experiment.ipynb`

### 실험 목적

같은 의도라도 사람마다 다르게 표현합니다. "췌장 분할해줘"와 "이자 찾아줘"는 같은 요청이지만 완전히 다른 표현입니다. 각 파이프라인이 이런 다양한 표현에 얼마나 강건한지 확인합니다.

### 알고자 했던 것

- 구어체나 비표준 표현("이자", "콩팥")에서도 Hybrid가 작동하는가?
- 명사만 입력하거나 의문형으로 물었을 때도 동작하는가?
- 어떤 표현에서 실패하는가?

### 방법

7가지 표현 변형을 3가지 파이프라인으로 처리, `pancreas_001` 단일 케이스 평가

### 결과

| 표현 | Raw Korean | General Trans | Hybrid |
|------|:---:|:---:|:---:|
| "췌장을 분할해줘" | 0.0000 | 0.0000 | **0.5037** |
| "이자를 찾아줘" (구어체) | 0.0000 | 오류 | 오류 |
| "췌장 부위를 세그멘테이션해줘" | 0.0000 | **0.5064** | **0.5037** |
| "췌장이 어디있어?" | 0.0000 | 0.0000 | **0.5037** |
| "췌장" (명사만) | 0.0000 | 오류 | **0.5048** |
| 영어 직접 입력 | **0.5037** | **0.5037** | **0.5037** |

### 발견

Hybrid 파이프라인은 표준 한국어 표현에서 일관되게 0.50+ 성능을 유지합니다. 그러나 구어체 장기명("이자")이나 혼합어("pancreas 분할해줘")에서는 오류가 발생합니다. 이는 Layer 2의 용어 사전에 해당 표현이 누락되었거나 전처리 단계에서 토큰화 오류가 발생하기 때문입니다. **구어체 및 혼합어 처리는 향후 개선 여지**가 있습니다.

---

## 실험 3: Threshold Sensitivity

**노트북:** `Med3D_Korean_Interface_Pipeline_Experiment.ipynb`

### 실험 목적

세그멘테이션 마스크는 모델이 출력한 확률 맵을 임계값으로 이진화해서 생성합니다. 이 임계값 설정이 성능에 얼마나 영향을 주는지, 최적값이 있는지 확인합니다.

### 알고자 했던 것

- 임계값에 따라 Dice가 크게 달라지는가?
- 최적 임계값은 얼마인가?

### 방법

Hybrid 파이프라인으로 `pancreas_001` 추론 후, 같은 확률 맵에 threshold 0.3~0.7 적용

### 결과

| Threshold | Dice | 예측 복셀 수 |
|:---------:|:----:|:----------:|
| 0.3 | 0.5025 | 73,332 |
| 0.4 | 0.5031 | 71,648 |
| **0.5** | **0.5037** | 70,036 |
| 0.6 | 0.5036 | 68,148 |
| 0.7 | 0.5033 | 66,220 |

### 발견

임계값 전 구간(0.3→0.7)에 걸쳐 Dice 차이가 **0.001 미만**으로 매우 안정적입니다. 임계값이 낮을수록 예측 복셀 수가 늘어 약간 과세그멘테이션되고, 높을수록 줄어들지만 성능 차이는 무시할 수준입니다. 기본값 **0.5**가 적절하며, 임계값 튜닝보다 파이프라인 개선이 훨씬 중요한 요인임을 확인했습니다.

---

## 실험 4: 프롬프트 엔지니어링 최적화

**노트북:** `M3D_KO_Prompt_Optimal.ipynb`

### 실험 목적

Hybrid 파이프라인이 생성하는 프롬프트가 최선인지 검증합니다. CoT, 공간 힌트, 구조화 포맷 등 다양한 프롬프트 전략이 M3D의 세그멘테이션 성능을 개선할 수 있는지 22종의 영문 프롬프트로 체계적으로 비교합니다.

### 알고자 했던 것

- Chain-of-Thought가 세그멘테이션에 도움이 되는가?
- 해부학적 위치 힌트를 주면 성능이 오르는가?
- JSON, key-value 같은 구조화 포맷이 더 명확한 조건화를 유도하는가?
- `segment` 대신 `identify`, `isolate` 같은 유사어도 작동하는가?

### 방법

22개 프롬프트 × 5케이스 = **110회 추론**

| 그룹 | 전략 |
|------|------|
| Baseline | M3D 학습 템플릿과 유사한 기준형 (3종) |
| Length | 프롬프트 길이가 성능에 미치는 영향 (4종) |
| Spatial | 해부학 위치 힌트 추가 (4종) |
| Lexical | `segment` 대신 유사어 사용 (4종) |
| Structured | JSON / key-value 구조화 포맷 (4종) |
| CoT | 단계별 추론 유도 (3종) |

### 결과

**그룹별 평균 Dice**

| 그룹 | 그룹평균 | 그룹최고 | 비고 |
|------|:-------:|:-------:|------|
| **Baseline** | **0.5500** | **0.5517** | 안정적이고 일관됨 |
| CoT | 0.5496 | 0.5507 | Baseline과 유의미한 차이 없음 |
| Spatial | 0.5162 | 0.5477 | 일부 케이스에서 오히려 성능 저하 |
| Length | 0.2739 | 0.5487 | 단어 수 적을수록 완전 실패 |
| Structured | 0.2185 | 0.4376 | 불안정, 케이스마다 편차 큼 |
| **Lexical** | **0.0000** | 0.0000 | 완전 실패 |

**Top 5 최고 프롬프트**

| 순위 | ID | 그룹 | 프롬프트 | Dice |
|:---:|:---:|:---:|---------|:---:|
| 1 | B2 | Baseline | Can you segment the pancreas in this image? | **0.5517** |
| 2 | B3 | Baseline | Please segment the pancreas in this image and output the mask. | 0.5513 |
| 3 | CoT2 | CoT | First identify abdominal organs, then segment the pancreas. | 0.5507 |
| 4 | CoT1 | CoT | Step 1: Locate the stomach. Step 2: Find the pancreas... | 0.5505 |
| 5 | L4 | Length | Please carefully identify and segment the pancreatic tissue... | 0.5487 |

### 발견

Hybrid 파이프라인(0.5515)을 유의미하게 초과하는 프롬프트 형식은 존재하지 않습니다. 특히 `segment` / `can you` 패턴이 없는 Lexical 변형(identify/isolate/extract)은 Dice 0.0000으로 완전 실패했습니다. M3D는 특정 학습 토큰 분포에 매우 강하게 의존하기 때문에, 프롬프트 엔지니어링보다 **엔티티 정규화(Layer 2)와 템플릿 선택(Layer 3)이 결정적인 요소**임이 재확인됩니다.

---

## 실험 5: Layer 1 의도 분류 Robustness

**노트북:** `Korean_Medical_Query_Transformation_Experiment.ipynb`

### 실험 목적

Layer 1은 규칙 기반 키워드 분류기입니다. 단순한 키워드 매칭이 실제 다양한 한국어 의료 표현에서 얼마나 정확하게 의도를 분류할 수 있는지 검증합니다. 만약 성능이 낮다면 ML 기반 분류기로의 전환을 검토해야 합니다.

### 알고자 했던 것

- 규칙 기반 분류기만으로 실용적인 수준(~90%)의 정확도를 달성할 수 있는가?
- 어떤 태스크에서 오분류가 집중되는가?
- 어떤 유형의 표현이 분류기를 혼동시키는가?

### 방법

50개 paraphrase 테스트셋 수동 구성 (SEG 15 · VQA 15 · REPORT 10 · REG 10), 의도 분류기 예측값과 비교

### 결과

**정확도: 92% (46/50), Macro F1: 0.92**

| 클래스 | Precision | Recall | F1 | 샘플 수 |
|--------|:---------:|:------:|:--:|:-------:|
| Segmentation | 1.00 | 0.90 | 0.95 | 15 |
| VQA | 0.91 | 1.00 | 0.95 | 15 |
| Report | 0.93 | 0.93 | 0.93 | 10 |
| REG | 0.87 | 0.87 | 0.87 | 10 |
| **Macro avg** | **0.93** | **0.92** | **0.92** | **50** |

**오분류 케이스 (4건)**

| 질문 | 정답 | 예측 | 원인 분석 |
|------|:---:|:---:|---------|
| "췌장만 보고싶어" | SEG | VQA | "보고싶어"에 SEG 키워드 없음 |
| "이상 소견 있나?" | VQA | Report | "소견" 키워드가 REPORT 규칙과 충돌 |
| "췌장이 정상으로 보여?" | VQA | SEG | "보여" 키워드가 SEG 규칙과 충돌 |
| "신장이 어떤 장기야?" | REG | VQA | REG 키워드 없이 VQA로 fallback |

![Confusion Matrix](docs/images/confusion_matrix.png)

### 발견

규칙 기반 분류기만으로 Macro F1 0.92를 달성했습니다. 오분류는 모두 의도가 경계에 걸친 표현에서 발생하며, 4건 모두 키워드 충돌 또는 키워드 부재 상황입니다. 한국어 의료 질의는 의도별로 비교적 명확한 어휘 패턴을 가지기 때문에, ML 기반 분류기 없이도 실용적인 수준을 달성할 수 있었습니다.

---

## 실험 6: Layer 3 Task Mismatch

**노트북:** `Korean_Medical_Query_Transformation_Experiment.ipynb`

### 실험 목적

Layer 3는 의도에 따라 적절한 M3D 프롬프트 템플릿을 선택합니다. 이 계층이 정말 필요한지, 즉 잘못된 템플릿을 사용했을 때 실제로 성능이 저하되는지 직접 측정합니다.

### 알고자 했던 것

- 장기명이 정확하게 전달되어도, 템플릿이 잘못되면 세그멘테이션이 실패하는가?
- 잘못된 템플릿 사용 시 모델이 어떤 응답을 생성하는가?

### 방법

세그멘테이션 질문("췌장을 분할해줘")에 3가지 태스크 템플릿을 각각 적용, `pancreas_001` 케이스에서 Dice 비교

### 결과

| 적용 템플릿 | Dice | 모델 응답 |
|------------|:----:|---------|
| **Seg 템플릿 (올바름)** | **0.5037** | `The segmentation reveals [SEG].` |
| VQA 템플릿 | 0.0000 | `"Atrophic pancreas with calcification."` |
| Report 템플릿 | 0.0000 | `"Numerous liver lesions with peripheral nodular enhancement..."` |

![Task Mismatch](docs/images/task_mismatch.png)

### 발견

장기명이 정확하게 전달된 경우라도, 잘못된 템플릿을 사용하면 모델이 `[SEG]` 토큰을 생성하지 않고 전혀 다른 태스크(VQA 응답, Report 생성)를 수행합니다. 특히 Report 템플릿 사용 시 모델이 해당 CT와 무관한 "간 병변" 소견을 출력하기도 했습니다. **Layer 3의 Task-aware Template Selection은 선택이 아니라 필수**입니다.

---

## 실험 7: Ablation Study

**노트북:** `Korean_Medical_Query_Transformation_Experiment.ipynb`

### 실험 목적

3계층 파이프라인에서 각 계층이 전체 성능에 얼마나 기여하는지 정량적으로 측정합니다. 각 계층을 하나씩 제거한 3가지 ablation 설정을 Full 파이프라인과 비교합니다.

### 알고자 했던 것

- 계층 간 기여도의 상대적 크기는 어떻게 되는가? (어떤 계층이 가장 중요한가?)
- 각 계층이 없을 때 단순히 성능이 낮아지는가, 아니면 완전히 실패하는가?
- 3계층 전부가 필수인가, 일부는 생략 가능한가?

### 방법

| 설정 | 처리 방식 |
|------|---------|
| Full (L1+L2+L3) | 전체 파이프라인 |
| −Layer 1 | 의도 분류 없이 3개 템플릿 중 **랜덤** 선택 |
| −Layer 2 | 엔티티 정규화 없이 일반 번역(GoogleTranslate) 사용 |
| −Layer 3 | 엔티티 정규화만 적용, 템플릿 없이 장기명만 전달 |

### 결과

| 설정 | 평균 Dice | 표준편차 |
|------|:---------:|:--------:|
| **Full (L1+L2+L3)** | **0.5515** | 0.0352 |
| −Layer 1 (랜덤 템플릿) | 0.1083 | 0.2167 |
| −Layer 2 (정규화 제거) | 0.0000 | 0.0000 |
| −Layer 3 (템플릿 제거) | 0.0000 | 0.0000 |

**−Layer 1 케이스별 상세 (랜덤성 확인)**

| 케이스 | 선택된 랜덤 템플릿 | Dice |
|--------|-----------------|:---:|
| pancreas_001 | VQA 템플릿 선택 | 0.0000 |
| pancreas_004 | **SEG 템플릿 선택** | 0.5417 |
| pancreas_005 | VQA 템플릿 선택 | 0.0000 |
| pancreas_006 | Report 템플릿 선택 | 0.0000 |
| pancreas_010 | Report 템플릿 선택 | 0.0000 |

![Ablation](docs/images/ablation.png)

### 발견

- **−Layer 2, −Layer 3**: 모두 Dice 0.0000으로 완전 실패. 두 계층 모두 없으면 파이프라인 자체가 작동하지 않습니다.
- **−Layer 1**: 평균 0.1083이지만 표준편차 0.2167로 매우 불안정합니다. pancreas_004에서 우연히 SEG 템플릿이 선택돼 0.5417을 기록했을 뿐, 나머지 케이스는 모두 0.0000입니다. 실제 운용에서는 3/4 확률로 완전히 실패하는 셈입니다.

**기여도 순위: Layer 2 > Layer 3 > Layer 1**. 세 계층 모두 제거 불가능한 필수 구성 요소이며, 특히 Layer 2(엔티티 정규화)가 전체 파이프라인의 핵심입니다.

---

## 종합 결론

### 연구 질문

> M3D-LaMed를 한국어 환경에서 실용적으로 사용할 수 있는가? 그렇다면 어떤 구조가 최적인가?

### 결론

**가능합니다.** 단, 단순 번역만으로는 불가능하며 3가지 계층이 모두 필요합니다.

| 계층 | 역할 | 없으면? |
|------|------|--------|
| **Layer 2 (엔티티 정규화)** | 한국어 장기명 → M3D 학습 어휘로 변환 | Dice 0.0000 |
| **Layer 3 (템플릿 선택)** | 태스크에 맞는 M3D 프롬프트 패턴 적용 | Dice 0.0000 |
| **Layer 1 (의도 분류)** | Layer 3에 올바른 템플릿 공급 | 예측 불가(σ=0.22) |

**핵심 발견:**
1. M3D는 `[SEG]` 토큰을 생성하기 위해 특정 프롬프트 패턴(`segment`, `can you`, `output the mask`)에 강하게 의존합니다.
2. 의미상 올바른 번역("Split the pancreas")도 패턴이 다르면 완전히 실패합니다.
3. 일반 번역기로는 이 문제를 해결할 수 없으며, **의료 도메인 특화 정규화**가 반드시 필요합니다.
4. 3계층 Full 파이프라인 Dice **0.5515**는 영어 최적 프롬프트(0.5517)와 동등한 수준입니다. 즉, 한국어 질의가 영어 직접 입력과 거의 동일한 성능을 냅니다.

### 한계 및 향후 과제

- 구어체 장기명("이자", "콩팥" 일부 변형)과 혼합어 처리 개선 필요
- 실험 샘플 수 5개로 통계적 유의성이 제한적 (M3D의 높은 추론 비용으로 인한 제약)
- 췌장 외 다른 장기에 대한 추가 검증 필요
- Layer 1을 ML 기반 분류기로 교체 시 경계 표현 오분류 감소 가능
